# ZagKit v2 native validation request

This branch exists only to execute the pull-request validation workflow against
the current `master` visual-system foundation.

Required evidence:

- exact pinned Zag compiler seed executes;
- visual material and lighting policy compiles and passes;
- SurfaceV2 compiles and preserves display, hit, semantics, focus, and Talkback identity;
- canonical Surface and design-token contracts remain green;
- the complete deterministic headless gate remains green.
